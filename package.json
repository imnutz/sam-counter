{
  "name": "sam",
  "version": "1.0.0",
  "description": "sam example",
  "scripts": {
    "build": "browserify app.js -o bundle.js -t [ babelify --presets [ es2015 stage-2] ]"
  },
  "author": "sonngoc",
  "license": "ISC",
  "dependencies": {
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-core": "^6.7.2",
    "babel-preset-es2015": "^6.6.0",
    "babelify": "^7.2.0"
  }
}
